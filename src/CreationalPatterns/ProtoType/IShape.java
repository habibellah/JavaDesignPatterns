package CreationalPatterns.ProtoType;
/*here the clone method its work will return a new obj of one of the concrete classes
who will implement it and with the same properties of the first one
 */
public interface IShape {
    public IShape clone() ;
}
